// var npm = require('./package.json');

// var dep = {};

// Object.keys(npm.dependencies).forEach(function(key) {
//   dep[key] = key;
// });

// module.exports = dep;
module.exports = {
  'react': 'react',
  'es6-promise': 'es6-promise',
  'quill': 'quill',
  'isomorphic-fetch': 'isomorphic-fetch'
  //'slug': 'slug'
};
